{
  "name": "surechigai-matching",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d postgres && sleep 3 && npm run db:migrate",
    "db:migrate": "docker compose exec -T postgres psql -U app -d surechigai -f /migrations/0001_init.sql",
    "dev:api": "npm run dev --workspace=api",
    "dev:web": "npm run dev --workspace=web"
  }
}
